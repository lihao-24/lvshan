# 闾山文化蜡烛网站 - 静态网站部署指南

## 📋 目录
1. [部署方案概述](#部署方案概述)
2. [准备工作](#准备工作)
3. [方案一：使用 Vercel 部署（推荐）](#方案一使用-vercel-部署推荐)
4. [方案二：使用 Netlify 部署](#方案二使用-netlify-部署)
5. [方案三：使用 GitHub Pages 部署](#方案三使用-github-pages-部署)
6. [方案四：使用云服务器部署](#方案四使用云服务器部署)
7. [配置 iframe 嵌入](#配置-iframe-嵌入)
8. [自定义域名配置](#自定义域名配置)
9. [常见问题解决](#常见问题解决)

---

## 部署方案概述

### 为什么使用 iframe 方案？
当平台对代码块长度有限制时，我们可以将完整网站部署到外部服务器，然后通过 iframe 嵌入到平台页面中。

### 优势：
- ✅ 突破平台代码长度限制
- ✅ 网站独立维护，更新方便
- ✅ 可以使用自定义域名
- ✅ 支持完整的网站功能

---

## 准备工作

### 需要准备的文件
确保你有完整的 `lushan-candle-website` 文件夹，包含以下内容：

```
lushan-candle-website/
├── index.html          # 主页面
├── css/                # 样式文件
│   ├── main.css
│   ├── responsive.css
│   └── variables.css
├── js/                 # JavaScript 文件
│   ├── main.js
│   ├── language.js
│   ├── carousel.js
│   └── fullpage-scroll.js
├── data/               # 数据文件
│   └── content.json
├── images/             # 图片资源
│   ├── backgrounds/
│   ├── brand/
│   ├── materials/
│   ├── products/
│   └── lihe.png
├── robots.txt          # SEO 配置
└── sitemap.xml         # 网站地图
```

---

## 方案一：使用 Vercel 部署（推荐）

### 🌟 推荐理由
- 完全免费
- 部署速度快
- 自动 HTTPS
- 支持自定义域名
- 全球 CDN 加速
- 操作简单

### 步骤 1：注册 Vercel 账号

1. 访问 [https://vercel.com](https://vercel.com)
2. 点击 "Sign Up" 注册账号
3. 推荐使用 GitHub 账号登录（更方便）

### 步骤 2：上传项目

#### 方法 A：通过 GitHub（推荐）

1. **创建 GitHub 仓库**
   - 访问 [https://github.com/new](https://github.com/new)
   - 仓库名称：`lvshan-candle-website`
   - 设置为 Public（公开）
   - 点击 "Create repository"

2. **上传文件到 GitHub**
   ```bash
   # 在终端中执行以下命令
   cd lushan-candle-website
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/你的用户名/lvshan-candle-website.git
   git push -u origin main
   ```

3. **在 Vercel 中导入项目**
   - 登录 Vercel
   - 点击 "Add New..." → "Project"
   - 选择 "Import Git Repository"
   - 选择你刚创建的仓库
   - 点击 "Deploy"

#### 方法 B：直接上传文件夹

1. 登录 Vercel
2. 点击 "Add New..." → "Project"
3. 选择 "Upload" 标签
4. 将 `lushan-candle-website` 文件夹拖拽到上传区域
5. 点击 "Deploy"

### 步骤 3：获取网站地址

部署完成后，Vercel 会自动生成一个地址，格式如下：
```
https://lvshan-candle-website.vercel.app
```

或者类似：
```
https://lvshan-candle-website-xxx.vercel.app
```

### 步骤 4：配置 iframe

将获取的地址填入 `iframe.html` 文件：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>闾山文化蜡烛</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body, html {
            width: 100%;
            height: 100%;
            overflow: hidden;
        }
        iframe {
            width: 100%;
            height: 100vh;
            border: none;
        }
    </style>
</head>
<body>
    <!-- 将下面的地址替换为你的 Vercel 地址 -->
    <iframe src="https://lvshan-candle-website.vercel.app"></iframe>
</body>
</html>
```

---

## 方案二：使用 Netlify 部署

### 步骤 1：注册 Netlify 账号

1. 访问 [https://www.netlify.com](https://www.netlify.com)
2. 点击 "Sign up" 注册账号
3. 推荐使用 GitHub 账号登录

### 步骤 2：部署网站

#### 方法 A：拖拽上传（最简单）

1. 登录 Netlify
2. 在首页找到 "Sites" 区域
3. 直接将 `lushan-candle-website` 文件夹拖拽到页面中的上传区域
4. 等待部署完成（通常 1-2 分钟）

#### 方法 B：通过 GitHub

1. 将项目上传到 GitHub（参考 Vercel 方案的步骤）
2. 在 Netlify 点击 "Add new site" → "Import an existing project"
3. 选择 "GitHub"
4. 授权并选择你的仓库
5. 点击 "Deploy site"

### 步骤 3：获取网站地址

部署完成后，Netlify 会生成一个地址：
```
https://random-name-123456.netlify.app
```

你可以在 "Site settings" → "Change site name" 中修改为：
```
https://lvshan-candle.netlify.app
```

### 步骤 4：配置 iframe

```html
<iframe src="https://lvshan-candle.netlify.app"></iframe>
```

---

## 方案三：使用 GitHub Pages 部署

### 步骤 1：上传到 GitHub

1. 创建 GitHub 仓库（参考 Vercel 方案）
2. 上传 `lushan-candle-website` 文件夹中的所有文件

### 步骤 2：启用 GitHub Pages

1. 进入仓库页面
2. 点击 "Settings"（设置）
3. 在左侧菜单找到 "Pages"
4. 在 "Source" 下拉菜单中选择 "main" 分支
5. 点击 "Save"

### 步骤 3：获取网站地址

GitHub Pages 会生成地址：
```
https://你的用户名.github.io/lvshan-candle-website/
```

### 步骤 4：配置 iframe

```html
<iframe src="https://你的用户名.github.io/lvshan-candle-website/"></iframe>
```

---

## 方案四：使用云服务器部署

### 适用场景
- 已有云服务器（阿里云、腾讯云、AWS 等）
- 需要完全控制服务器
- 需要自定义服务器配置

### 步骤 1：准备服务器

确保服务器已安装：
- Nginx 或 Apache
- 已开放 80 和 443 端口

### 步骤 2：上传文件

#### 使用 FTP/SFTP 工具（推荐 FileZilla）

1. 下载并安装 [FileZilla](https://filezilla-project.org/)
2. 连接到服务器：
   - 主机：你的服务器 IP
   - 用户名：服务器用户名
   - 密码：服务器密码
   - 端口：22（SFTP）
3. 将 `lushan-candle-website` 文件夹上传到服务器的 web 目录
   - Nginx 默认：`/usr/share/nginx/html/`
   - Apache 默认：`/var/www/html/`

#### 使用命令行（SCP）

```bash
# 压缩文件夹
cd /Users/ella/Desktop/lvshan-candles
zip -r lushan-candle-website.zip lushan-candle-website/

# 上传到服务器
scp lushan-candle-website.zip 用户名@服务器IP:/usr/share/nginx/html/

# SSH 登录服务器
ssh 用户名@服务器IP

# 解压文件
cd /usr/share/nginx/html/
unzip lushan-candle-website.zip
```

### 步骤 3：配置 Nginx

创建或编辑 Nginx 配置文件：

```bash
sudo nano /etc/nginx/sites-available/lvshan-candle
```

添加以下配置：

```nginx
server {
    listen 80;
    server_name 你的域名.com;  # 或使用 IP 地址
    
    root /usr/share/nginx/html/lushan-candle-website;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 启用 gzip 压缩
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    
    # 缓存静态资源
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

启用配置并重启 Nginx：

```bash
sudo ln -s /etc/nginx/sites-available/lvshan-candle /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 步骤 4：获取访问地址

- 如果使用 IP：`http://你的服务器IP/`
- 如果配置了域名：`http://你的域名.com/`

### 步骤 5：配置 HTTPS（推荐）

使用 Let's Encrypt 免费证书：

```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书并自动配置
sudo certbot --nginx -d 你的域名.com
```

---

## 配置 iframe 嵌入

### 基础 iframe 配置

创建 `iframe.html` 文件：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>闾山文化蜡烛</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body, html {
            width: 100%;
            height: 100%;
            overflow: hidden;
        }
        iframe {
            width: 100%;
            height: 100vh;
            border: none;
        }
    </style>
</head>
<body>
    <!-- 替换为你的网站地址 -->
    <iframe src="https://你的网站地址.com"></iframe>
</body>
</html>
```

### 高级配置选项

#### 1. 指定初始显示的页面部分

```html
<!-- 直接跳转到产品页面 -->
<iframe src="https://你的网站地址.com/#products"></iframe>

<!-- 跳转到联系页面 -->
<iframe src="https://你的网站地址.com/#contact"></iframe>
```

#### 2. 添加加载动画

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>闾山文化蜡烛</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body, html {
            width: 100%;
            height: 100%;
            overflow: hidden;
        }
        iframe {
            width: 100%;
            height: 100vh;
            border: none;
            opacity: 0;
            transition: opacity 0.5s;
        }
        iframe.loaded {
            opacity: 1;
        }
        .loading {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 18px;
            color: #8B6F47;
        }
        .loading.hidden {
            display: none;
        }
    </style>
</head>
<body>
    <div class="loading">加载中...</div>
    <iframe src="https://你的网站地址.com" onload="this.classList.add('loaded'); document.querySelector('.loading').classList.add('hidden');"></iframe>
</body>
</html>
```

#### 3. 响应式高度调整

```html
<style>
    iframe {
        width: 100%;
        height: 100vh;
        border: none;
    }
    
    /* 移动端优化 */
    @media (max-width: 768px) {
        iframe {
            height: calc(100vh - 60px); /* 为移动端导航栏预留空间 */
        }
    }
</style>
```

---

## 自定义域名配置

### 为什么需要自定义域名？
- 更专业的品牌形象
- 更容易记忆
- 提升用户信任度

### 步骤 1：购买域名

推荐域名注册商：
- [阿里云（万网）](https://wanwang.aliyun.com/)
- [腾讯云](https://dnspod.cloud.tencent.com/)
- [GoDaddy](https://www.godaddy.com/)
- [Namecheap](https://www.namecheap.com/)

推荐域名：
- `lvshan-candle.com`
- `lvshanculture.com`
- `lvshan.art`

### 步骤 2：配置 DNS

#### 在 Vercel 中配置

1. 进入 Vercel 项目设置
2. 点击 "Domains"
3. 输入你的域名，点击 "Add"
4. Vercel 会提供 DNS 记录配置信息

5. 在域名注册商处添加 DNS 记录：
   ```
   类型: CNAME
   名称: www
   值: cname.vercel-dns.com
   
   类型: A
   名称: @
   值: 76.76.21.21
   ```

#### 在 Netlify 中配置

1. 进入 Netlify 项目设置
2. 点击 "Domain management"
3. 点击 "Add custom domain"
4. 输入域名并验证

5. 在域名注册商处添加 DNS 记录：
   ```
   类型: CNAME
   名称: www
   值: 你的项目名.netlify.app
   
   类型: A
   名称: @
   值: 75.2.60.5
   ```

#### 在云服务器中配置

在域名注册商处添加 A 记录：
```
类型: A
名称: @
值: 你的服务器IP地址

类型: A
名称: www
值: 你的服务器IP地址
```

### 步骤 3：等待 DNS 生效

DNS 解析通常需要 10 分钟到 48 小时生效。可以使用以下工具检查：
- [DNS Checker](https://dnschecker.org/)
- 命令行：`nslookup 你的域名.com`

### 步骤 4：启用 HTTPS

大多数平台会自动配置 HTTPS：
- Vercel：自动配置
- Netlify：自动配置
- 云服务器：使用 Let's Encrypt（参考上文）

---

## 常见问题解决

### 1. iframe 显示空白

**可能原因：**
- 网站地址错误
- 目标网站设置了 X-Frame-Options 限制

**解决方法：**
```html
<!-- 检查地址是否正确 -->
<iframe src="https://正确的网站地址.com"></iframe>

<!-- 在服务器配置中允许 iframe 嵌入 -->
<!-- Nginx 配置 -->
add_header X-Frame-Options "SAMEORIGIN";
# 或允许所有来源
add_header X-Frame-Options "ALLOWALL";
```

### 2. 样式或图片加载失败

**可能原因：**
- 文件路径错误
- 资源使用了相对路径

**解决方法：**
- 确保所有文件都已上传
- 检查文件路径大小写（Linux 服务器区分大小写）
- 在浏览器开发者工具中查看具体错误

### 3. 移动端显示异常

**解决方法：**
```html
<!-- 确保添加了 viewport meta 标签 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<!-- iframe 添加移动端优化 -->
<style>
    @media (max-width: 768px) {
        iframe {
            width: 100%;
            min-height: 100vh;
        }
    }
</style>
```

### 4. 跨域问题

**解决方法：**

在服务器配置中添加 CORS 头：

```nginx
# Nginx 配置
add_header Access-Control-Allow-Origin "*";
add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
```

### 5. 网站加载速度慢

**优化方法：**
1. 压缩图片（使用 TinyPNG 或 ImageOptim）
2. 启用 CDN 加速
3. 启用 gzip 压缩
4. 使用浏览器缓存

---

## 部署检查清单

部署完成后，请检查以下项目：

- [ ] 网站可以正常访问
- [ ] 所有图片都能正常显示
- [ ] CSS 样式正确加载
- [ ] JavaScript 功能正常工作
- [ ] 语言切换功能正常
- [ ] 全屏滚动功能正常
- [ ] 产品页内部滚动正常
- [ ] 移动端显示正常
- [ ] HTTPS 已启用（推荐）
- [ ] 自定义域名已配置（可选）
- [ ] iframe 嵌入正常显示

---

## 推荐部署方案对比

| 特性 | Vercel | Netlify | GitHub Pages | 云服务器 |
|------|--------|---------|--------------|----------|
| 难度 | ⭐ 简单 | ⭐ 简单 | ⭐⭐ 中等 | ⭐⭐⭐ 复杂 |
| 费用 | 免费 | 免费 | 免费 | 付费 |
| 速度 | 很快 | 很快 | 快 | 取决于配置 |
| CDN | ✅ | ✅ | ✅ | 需自行配置 |
| 自动 HTTPS | ✅ | ✅ | ✅ | 需自行配置 |
| 自定义域名 | ✅ | ✅ | ✅ | ✅ |
| 部署时间 | 1-2 分钟 | 1-2 分钟 | 5-10 分钟 | 30+ 分钟 |

**新手推荐：Vercel 或 Netlify**

---

## 技术支持

如有问题，请联系：
- 邮箱：lvshan.customerservice@gmail.com
- 或查看项目文档

---

## 更新日志

- 2026-01-27：创建初始版本
- 包含 4 种主流部署方案
- 添加详细的 iframe 配置说明
- 提供完整的故障排查指南
